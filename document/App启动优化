1、减少Application的onCreate（）方法中的逻辑，只保留必要的初始化逻辑。onCreate（）方法会在APP启动时执行，如果逻辑过多会影响启动速度。

2、将不需要立即初始化的组建延迟初始化，比如延迟初始化SDK，延迟加载非必要页面等。可以在第一个Activity的onCreate（）中完成

3、对共享元素集页面初始化坐懒加载，只在需要的时候才去加载，不要一开始就全部加载

4、图片资源压缩，移除无用的资源。缩减安装包的体积可以提高启动速度

5、开启 Instant Run，可以快速迭代编译，不需要全量打包和安装

6、使用古镜像，避免每次冷启动都要走dex优化流程

7、针对频繁启动的activity进行优化，尽量复用，减少初始化的工作

8、使用自定义的Application 和 Activity管理启动流程，避免重复实例化对象

9、合理设置 VM heap size ，不要给予过大内存

10、使用Traceview 等工具分析启动过程，找到主线程卡顿点。
