⼀次完整的 Binder IPC 通信过程通常是这样：
1、⾸先 Binder 驱动在内核空间创建⼀个 数据接收缓存区 ；
2、接着在内核空间开辟⼀块内核缓存区，建⽴ 内核缓存区 和 内核中数据接收缓存区 之间的映射关系，
以及 内核中数据接收缓存区 和 接收进程⽤户空间地址 的映射关系；
3、发送⽅进程通过系统调⽤ copyfromuser() 将数据 copy 到内核中的内核缓存区，由于内核缓存区和接收进程的⽤户空间存在内存映射，
因此也就相当于把数据发送到了接收进程的⽤户空间，这样便完成了⼀次进程间的通信

android 跨进程通讯的方式：
管道、消息队列、共享内存 和 Socket 等

为何android用binder
性能
    Socket：作为⼀款通⽤接⼜，其传输效率低，开销⼤，主要⽤在跨⽹络的进程间通信和本机上进程间的低速通信。2次
    消息队列和管道：采⽤存储-转发⽅式，即数据先从发送⽅缓存区拷贝到内核开辟的缓存区中，然后再从内核缓存区拷贝到接收⽅缓存区，⾄少有2次拷贝过程。
    共享内存：虽然⽆需拷贝，但控制复杂，难以使⽤。0次
    Binder：只需要⼀次数据拷贝，性能上仅次于共享内存。 1次

稳定性
    再说说稳定性，Binder 基于 C/S 架构，客户端（Client）有什么需求就丢给服务端（Server）去完成，架构清晰、职责明确又相互独⽴，⾃然稳定
    性更好。共享内存虽然⽆需拷贝，但是控制负责，难以使⽤。从稳定性的⾓度讲，Binder 机制是优于共享内存的。

安全性
    Android 作为⼀个开放性的平台，市场上有各类海量的应⽤供⽤户选择安装，因此安全性对于 Android 平台⽽⾔极其重要。作为⽤户当然不希望
    我们下载的 APP 偷偷读取我的通信录，上传我的隐私数据，后台偷跑流量、消耗⼿机电量。传统的 IPC 没有任何安全措施，完全依赖上层协议来确
    保。⾸先传统的 IPC 接收⽅⽆法获得对⽅可靠的进程⽤户ID/进程ID（UID/PID），从⽽⽆法鉴别对⽅⾝份。Android 为每个安装好的 APP 分配了⾃
    ⼰的 UID，故⽽进程的 UID 是鉴别进程⾝份的重要标志。传统的 IPC 只能由⽤户在数据包中填⼊ UID/PID，但这样不可靠，容易被恶意程序利⽤。
    可靠的⾝份标识只有由 IPC 机制在内核中添加。其次传统的 IPC 访问接⼊点是开放的，只要知道这些接⼊点的程序都可以和对端建⽴连接，不管怎样
    都⽆法阻⽌恶意程序通过猜测接收⽅地址获得连接。同时 Binder 既⽀持实名 Binder，又⽀持匿名 Binder，安全性⾼。
